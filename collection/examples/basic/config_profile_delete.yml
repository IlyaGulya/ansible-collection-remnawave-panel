# AUTOMATICALLY GENERATED - DO NOT EDIT
# Generated by remnawave-ansible-gen
#
# Example: Delete a Config Profile
# Usage: ansible-playbook config_profile_delete.yml -e @../vars/example_vars.yml
---
- name: Delete Config Profile
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../vars/example_vars.yml

  collections:
    - remnawave.panel

  tasks:
    - name: Delete config profile by name
      remnawave.panel.config_profile:
        api_url: "{{ remnawave_api_url }}"
        api_token: "{{ remnawave_api_token }}"
        state: absent
        name: "{{ config_profile_name }}"
      register: result

    - name: Display result
      ansible.builtin.debug:
        var: result