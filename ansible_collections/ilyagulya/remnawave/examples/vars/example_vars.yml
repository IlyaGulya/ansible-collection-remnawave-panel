# AUTOMATICALLY GENERATED - DO NOT EDIT
# Generated by remnawave-ansible-gen
#
# Example variables for Remnawave Ansible modules
# Copy this file and customize the values for your environment
---
# API connection settings
remnawave_api_url: "https://panel.example.com"
remnawave_api_token: "your-api-token-here"

# Config profile settings
config_profile_name: "Production Profile"

# Minimal valid Xray configuration (JSON syntax, parsed as object)
# Note: Config profiles must have at least one inbound
xray_config:
  {
    "log": {
      "loglevel": "info"
    },
    "inbounds": [
      {
        "tag": "MainInbound",
        "port": 10000,
        "protocol": "shadowsocks",
        "settings": {
          "clients": [],
          "network": "tcp,udp"
        },
        "sniffing": {
          "enabled": true,
          "destOverride": ["http", "tls"]
        }
      }
    ],
    "outbounds": [
      {
        "tag": "DIRECT",
        "protocol": "freedom"
      }
    ],
    "routing": {
      "rules": []
    }
  }

# Node settings
node_name: "Production Node 1"
node_address: "10.0.0.1"
node_port: 443

# Multi-region node configuration for loop-based deployment
nodes:
  - name: "US East Node"
    address: "10.0.1.1"
    port: 443
    country_code: "US"
  - name: "EU West Node"
    address: "10.0.2.1"
    port: 443
    country_code: "DE"
  - name: "Asia Pacific Node"
    address: "10.0.3.1"
    port: 443
    country_code: "SG"

# Traffic tracking settings
traffic_tracking:
  is_active: true
  limit_bytes: 10737418240  # 10 GB
  notify_percent: 80
  reset_day: 1
  consumption_multiplier: 1.0
