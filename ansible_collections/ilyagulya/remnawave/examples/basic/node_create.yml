# AUTOMATICALLY GENERATED - DO NOT EDIT
# Generated by remnawave-ansible-gen
#
# Example: Create a Node
# Usage: ansible-playbook node_create.yml -e @../vars/example_vars.yml
---
- name: Create Node
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../vars/example_vars.yml

  tasks:
    - name: Create node
      ilyagulya.remnawave.node:
        api_url: "{{ remnawave_api_url }}"
        api_token: "{{ remnawave_api_token }}"
        state: present
        name: "{{ node_name }}"
        address: "{{ node_address }}"
        port: "{{ node_port }}"
        config_profile:
          active_config_profile: "{{ config_profile_name }}"
          active_inbound_tags:
            - "MainInbound"
      register: result

    - name: Display result
      ansible.builtin.debug:
        var: result
