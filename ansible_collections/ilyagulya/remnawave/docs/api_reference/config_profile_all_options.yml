# AUTOMATICALLY GENERATED - DO NOT EDIT
# Generated by remnawave-ansible-gen
#
# Config Profile - All Available Options
# Shows every field supported by the config_profile module
---
- name: "Config Profile - all options example"
  hosts: localhost
  gather_facts: false
  vars:
    panel_url: "https://panel.example.com"
    api_token: "your-api-token-here"

  tasks:
    - name: Create config profile with all options
      ilyagulya.remnawave.config_profile:
        panel_url: "{{ panel_url }}"
        api_token: "{{ api_token }}"
        state: present
        name: "example-name"  # required, str, minLength: 2, maxLength: 30
        config:  # required, dict
          {
            "inbounds": [
              {
                "tag": "vless-tcp",
                "port": 443,
                "protocol": "vless",
                "settings": {
                  "clients": [],
                  "decryption": "none"
                }
              }
            ],
            "outbounds": [
              {
                "tag": "DIRECT",
                "protocol": "freedom"
              }
            ],
            "routing": {
              "rules": []
            }
          }
