---
# Panel URL for E2E tests (Caddy reverse proxy)
panel_url: "http://localhost:8080"

# Note: api_token is NOT defined here because:
# 1. It's generated during the create phase
# 2. Playbooks already define it via lookup:
#    {{ lookup('file', molecule_scenario_directory + '/.api-token') | trim }}
