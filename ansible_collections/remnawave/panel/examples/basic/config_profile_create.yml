# AUTOMATICALLY GENERATED - DO NOT EDIT
# Generated by remnawave-ansible-gen
#
# Example: Create a Config Profile
# Usage: ansible-playbook config_profile_create.yml -e @../vars/example_vars.yml
---
- name: Create Config Profile
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../vars/example_vars.yml

  collections:
    - remnawave.panel

  tasks:
    - name: Create config profile
      remnawave.panel.config_profile:
        api_url: "{{ remnawave_api_url }}"
        api_token: "{{ remnawave_api_token }}"
        state: present
        name: "{{ config_profile_name }}"
        config: "{{ xray_config }}"
      register: result

    - name: Display result
      ansible.builtin.debug:
        var: result